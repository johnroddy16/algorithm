# linked lists, constructors:

# class LinkedList:
    # def __init__(self, value):  # create a new node and initialize a new linked list
    # def append(self, value):  # create new node and add node to end
    # def prepend(self, value):  # create new node and add node to front
    # def insert(self, index, value):  # create new node and insert node at specified index

# all of these methodes create a new node and we do not want to write the code
# for creating a new node 4 different times so we create a class just for creating new nodes

# we will create a node class in node.py:




# class LinkedList:
#     def __init__(self, value):
#         new_node = Node(value)